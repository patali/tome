Copy of `extension/fonts/` — Go cannot embed across module boundaries, and the
PDF renderer must not fetch fonts over the network. Both families are SIL Open
Font License 1.1; the notices are alongside. Update both copies together.
