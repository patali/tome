# Copy to server/.env (gitignored) and fill in — push.sh loads it automatically.
#
# Classic GitHub PAT with write:packages. Fine-grained tokens do not work with
# GHCR.  Generate: https://github.com/settings/tokens/new?scopes=write:packages
GHCR_PAT=
